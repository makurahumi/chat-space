.header
  =render "hader"

.contents
  .sub-menu
    =render "property"
    =render "caption"
  
  .main-menu
    =render "group-home"
    =render "comment"
    .send-space
      .send-space__form
        %input{:placeholder => "type a comment"}
        %input{:type => "sbumit", :value => "SEND"}
